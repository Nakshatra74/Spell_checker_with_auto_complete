# Spell_checker_with_auto_complete
Trie-based spell checker and auto-complete engine in C++ — combines a hash-mapped trie, DFS traversal, and incremental Levenshtein-distance search for fast prefix completion and typo correction.
